const proxy = require('http-proxy-middleware')
const {isProd,isLocal, baseUrl,apiUrl,apiOrderList} = require('../env_config.js')
const util = require('../src/server/util')

module.exports = function (app) { 
    
}